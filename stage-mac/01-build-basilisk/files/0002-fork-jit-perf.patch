--- a/BasiliskII/src/Unix/main_unix.cpp	2026-06-16 05:46:28.000000000 -0400
+++ b/BasiliskII/src/Unix/main_unix.cpp	2026-06-16 07:14:53.866890773 -0400
@@ -24,6 +24,7 @@
 #include <stdlib.h>
 #include <signal.h>
 #include <errno.h>
+#include <time.h>
 #include <sstream>
 
 #ifdef USE_SDL
@@ -361,6 +362,154 @@
 
 
 /*
+ *  RPi-Mac performance telemetry (env-gated, near-zero overhead)
+ *
+ *  Reports sustained 68k instruction throughput (MIPS) and video frame rate
+ *  once per second. Instruction counting piggybacks on the existing
+ *  cpu_do_check_ticks() callback, which the interpreter already invokes once
+ *  per 65536 executed instructions, so the per-instruction hot loop gains no
+ *  new work. Everything is gated behind the RPIMAC_TELEMETRY env var, so a
+ *  production build with telemetry off pays only one branch per 65536
+ *  instructions. Set RPIMAC_TELEMETRY=1 to enable; RPIMAC_TELEMETRY_FILE=path
+ *  optionally redirects output away from stderr.
+ */
+
+uint64 rpimac_tele_insns = 0;			// total 68k instructions executed
+volatile uint64 rpimac_tele_frames = 0;	// video frames presented (set by the video thread)
+
+static int rpimac_tele_state = -1;		// -1 unknown, 0 off, 1 on
+static FILE *rpimac_tele_out = NULL;
+// Instructions represented by one cpu_do_check_ticks() call. The interpreter
+// calls it once per 65536 instructions (uint16 emulated_ticks wrap); the
+// AArch64 JIT injects a call every JIT_TICK_INTERVAL (64) compiled
+// instructions. We pick the right step so MIPS is meaningful in both cores
+// (approximate under JIT, since interpreter-fallback blocks still wrap at
+// 65536). Overridable via RPIMAC_TICK_STEP.
+static uint32 rpimac_tele_step = 65536;
+
+static void rpimac_telemetry_init(void)
+{
+	const char *e = getenv("RPIMAC_TELEMETRY");
+	bool on = (e != NULL && e[0] != '\0' && e[0] != '0');
+	if (!on) {
+		rpimac_tele_state = 0;
+		return;
+	}
+	const char *path = getenv("RPIMAC_TELEMETRY_FILE");
+	if (path != NULL && path[0] != '\0')
+		rpimac_tele_out = fopen(path, "we");
+	if (rpimac_tele_out == NULL)
+		rpimac_tele_out = stderr;
+#ifdef USE_JIT
+	if (PrefsFindBool("jit"))
+		rpimac_tele_step = 64;		// JIT_TICK_INTERVAL
+#endif
+	const char *s = getenv("RPIMAC_TICK_STEP");
+	if (s != NULL && s[0] != '\0')
+		rpimac_tele_step = (uint32)strtoul(s, NULL, 10);
+	rpimac_tele_state = 1;
+}
+
+static inline uint64 rpimac_now_ns(void)
+{
+	struct timespec ts;
+	clock_gettime(CLOCK_MONOTONIC, &ts);
+	return (uint64)ts.tv_sec * 1000000000ULL + (uint64)ts.tv_nsec;
+}
+
+static double rpimac_env_double(const char *name, double dflt)
+{
+	const char *v = getenv(name);
+	if (v == NULL || v[0] == '\0')
+		return dflt;
+	return atof(v);
+}
+
+#ifdef RPIMAC_PGO_DUMP
+// PGO profiling builds: the emulator is killed with SIGKILL on restart, so the
+// libgcov atexit handler never runs. Flush the accumulated profile periodically
+// so a fresh .gcda survives the kill. Compiled in only for -DRPIMAC_PGO_DUMP.
+extern "C" void __gcov_dump(void);
+#endif
+
+// Called from cpu_do_check_ticks(); each call represents rpimac_tele_step
+// executed 68k instructions (see above).
+static void rpimac_telemetry_quantum(void)
+{
+	if (rpimac_tele_state < 0)
+		rpimac_telemetry_init();
+	if (rpimac_tele_state == 0)
+		return;
+
+	rpimac_tele_insns += rpimac_tele_step;
+
+	static uint64 t0_ns = 0;
+	static uint64 last_ns = 0;
+	static uint64 last_insns = 0;
+	static uint64 last_frames = 0;
+	static uint64 boot_last_active_ns = 0;
+	static bool boot_reported = false;
+	static double active_fps = -1.0;
+	static double settle_s = -1.0;
+
+	uint64 now = rpimac_now_ns();
+	if (last_ns == 0) {
+		t0_ns = now;
+		last_ns = now;
+		last_insns = rpimac_tele_insns;
+		last_frames = rpimac_tele_frames;
+		active_fps = rpimac_env_double("RPIMAC_BOOT_ACTIVE_FPS", 3.0);
+		settle_s = rpimac_env_double("RPIMAC_BOOT_SETTLE_S", 3.0);
+		return;
+	}
+	uint64 dt = now - last_ns;
+	if (dt < 1000000000ULL)
+		return;
+
+	double secs = (double)dt / 1e9;
+	double mips = ((double)(rpimac_tele_insns - last_insns) / secs) / 1e6;
+	double fps = (double)(rpimac_tele_frames - last_frames) / secs;
+	double elapsed = (double)(now - t0_ns) / 1e9;
+
+	fprintf(rpimac_tele_out,
+		"rpimac-telemetry t=%.1f mips=%.2f fps=%.1f insns=%llu frames=%llu step=%u\n",
+		elapsed, mips, fps,
+		(unsigned long long)rpimac_tele_insns,
+		(unsigned long long)rpimac_tele_frames,
+		(unsigned)rpimac_tele_step);
+	fflush(rpimac_tele_out);
+
+	// Boot-to-steady-state detection: the screen is busy while booting (Happy
+	// Mac, the marching extensions, the desktop being drawn) and goes quiet
+	// once the Finder desktop is up and idle. Track the last "active" second
+	// and, after a settle window of quiet, report when the desktop was ready.
+	if (fps >= active_fps)
+		boot_last_active_ns = now;
+	if (!boot_reported && boot_last_active_ns != 0 &&
+	    (double)(now - boot_last_active_ns) / 1e9 >= settle_s) {
+		double boot_s = (double)(boot_last_active_ns - t0_ns) / 1e9;
+		fprintf(rpimac_tele_out,
+			"rpimac-telemetry boot_complete_s=%.1f steady_mips=%.2f\n",
+			boot_s, mips);
+		fflush(rpimac_tele_out);
+		boot_reported = true;
+	}
+
+#ifdef RPIMAC_PGO_DUMP
+	static int pgo_ctr = 0;
+	if (++pgo_ctr >= 10) {		// flush the cumulative profile ~every 10 s
+		pgo_ctr = 0;
+		__gcov_dump();
+	}
+#endif
+
+	last_ns = now;
+	last_insns = rpimac_tele_insns;
+	last_frames = rpimac_tele_frames;
+}
+
+
+/*
  *  Update virtual clock and trigger interrupts if necessary
  */
 
@@ -423,6 +572,12 @@
 uint16 emulated_ticks;
 void cpu_do_check_ticks(void)
 {
+	// Telemetry hook. The interpreter reaches here once per 65536 instructions
+	// (emulated_ticks uint16 wrap); under the JIT it is also called every
+	// JIT_TICK_INTERVAL compiled instructions. rpimac_telemetry_quantum()
+	// accounts for the right step internally.
+	rpimac_telemetry_quantum();
+
 	static int delay = -1;
 	if (delay < 0)
 		delay = PrefsFindInt32("delay");
--- a/BasiliskII/src/SDL/video_sdl2.cpp	2026-06-16 07:10:58.146188972 -0400
+++ b/BasiliskII/src/SDL/video_sdl2.cpp	2026-06-16 06:54:38.536388721 -0400
@@ -1419,6 +1419,13 @@
 	
     // Update the display
 	SDL_RenderPresent(sdl_renderer);
+
+	// RPi-Mac performance telemetry: count presented frames (read once/sec by
+	// the telemetry reporter in main_unix.cpp; benign cross-thread counter).
+	{
+		extern volatile uint64 rpimac_tele_frames;
+		rpimac_tele_frames++;
+	}
 	{
 		static bool dump_first_present_done = false;
 		const char *dump_first_present = getenv("B2_DUMP_FIRST_PRESENT");
--- a/BasiliskII/src/Unix/sysdeps.h	2026-06-16 05:46:28.000000000 -0400
+++ b/BasiliskII/src/Unix/sysdeps.h	2026-06-16 06:53:26.336216430 -0400
@@ -548,6 +548,14 @@
 
 #endif /* CPU_CAN_ACCESS_UNALIGNED */
 
+/* Raw (unswapped) 16-bit read for little-endian hosts. Pairs with a
+ * pre-swapped dispatch table when the build defines HAVE_GET_WORD_UNSWAPPED,
+ * dropping one byteswap per opcode fetch. (RPi-Mac: defined for all
+ * little-endian branches, e.g. aarch64, not just x86.) */
+#ifndef do_get_mem_word_unswapped
+#define do_get_mem_word_unswapped(a) ((uae_u32)*((uae_u16 *)(a)))
+#endif
+
 #endif /* WORDS_BIGENDIAN */
 
 #ifndef HAVE_OPTIMIZED_BYTESWAP_32
--- a/BasiliskII/src/Unix/configure.ac	2026-06-16 05:46:28.000000000 -0400
+++ b/BasiliskII/src/Unix/configure.ac	2026-06-16 06:53:40.068249945 -0400
@@ -64,6 +64,13 @@
 [ FPE_CORE_TEST_ORDER="ieee uae"
 ])
 
+dnl RPi-Mac: on ARM the IEEE fpu core defaults to the (slow) MPFR
+dnl arbitrary-precision backend. --enable-fpu-ieee forces the fast host
+dnl IEEE core (fpu_ieee.cpp, hardware double on aarch64) there instead.
+AC_ARG_ENABLE(fpu-ieee,
+[  --enable-fpu-ieee       on ARM, use the host IEEE fpu core instead of MPFR [default=no]],
+[WANT_FPU_IEEE=$enableval], [WANT_FPU_IEEE=no])
+
 dnl Addressing modes.
 AC_ARG_ENABLE(addressing,
 [  --enable-addressing=AM  specify the addressing mode to use [default=fastest]],
@@ -1994,7 +2001,7 @@
     case $ac_cv_c_float_format in
     IEEE*)
       if [[ "$UAE_PATH" = "../uae_cpu_2026" ]]; then
-        if [[ "$target_cpu" = "arm" -o "$target_cpu" = "aarch64" ]]; then
+        if [[ "$target_cpu" = "arm" -o "$target_cpu" = "aarch64" ]] && [[ "x$WANT_FPU_IEEE" != "xyes" ]]; then
           FPE_CORE="IEEE fpu core (MPFR)"
           DEFINES="$DEFINES -DFPU_MPFR"
           FPUSRCS="$UAE_PATH/fpu/fpu_mpfr.cpp"
