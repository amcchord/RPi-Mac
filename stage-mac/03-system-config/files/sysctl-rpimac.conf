# Flush dirty pages quickly so an abrupt power-off loses at most ~1-2s of
# Mac disk writes (protects the bootability/"blessing" of the disk images).
vm.dirty_writeback_centisecs = 100
vm.dirty_expire_centisecs = 200
