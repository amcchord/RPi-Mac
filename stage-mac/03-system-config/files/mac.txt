# ===================================================================
#  RPi-Mac configuration
#
#  Edit this file from any computer, then put the SD card back in the
#  Pi and power on. Changes are applied during boot.
# ===================================================================

# --- WiFi -----------------------------------------------------------
# Network name and password. Leave blank to keep the current network.
WIFI_SSID=__WIFI_SSID__
WIFI_PASS=__WIFI_PASS__

# Two-letter regulatory country code (US, GB, DE, ...)
WIFI_COUNTRY=__WIFI_COUNTRY__

# --- Display --------------------------------------------------------
# DISPLAY=hdmi   Standard HDMI output (default)
# DISPLAY=dpi28  Waveshare 2.8inch DPI LCD (480x640 panel)
DISPLAY=__DISPLAY_DEFAULT__

# Screen rotation in degrees: 0, 90, 180 or 270.
# Leave blank for automatic (90 on the DPI LCD, 0 on HDMI).
ROTATE=

# --- Debugging ------------------------------------------------------
# Set DEBUG=1 to show the Linux boot console instead of the Mac splash.
DEBUG=0
